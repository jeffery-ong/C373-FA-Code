<!DOCTYPE html>
<html>
<head>
  <title>ZacDelivery | <%= trackingId %></title>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link
    href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700&family=Space+Grotesk:wght@500;600;700&display=swap"
    rel="stylesheet"
  />
  <link
    rel="stylesheet"
    href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
    integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY="
    crossorigin=""
  />

  <style>
    :root {
      color-scheme: light;
      --bg: #f4f7ff;
      --surface: #ffffff;
      --text: #0f1a2b;
      --text-muted: #5a6a85;
      --primary: #2f6bff;
      --border: #dde5f4;
      --shadow: 0 10px 22px rgba(15, 26, 43, 0.08);
    }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      font-family: "Manrope", system-ui, -apple-system, sans-serif;
      background: var(--bg);
      color: var(--text);
      min-height: 100vh;
    }

    a { color: inherit; text-decoration: none; }

    .page { min-height: 100vh; display: flex; flex-direction: column; }

    .site-header {
      position: sticky;
      top: 0;
      z-index: 10;
      background: rgba(255, 255, 255, 0.95);
      border-bottom: 1px solid var(--border);
    }

    .header-inner {
      max-width: 1100px;
      margin: 0 auto;
      padding: 16px 24px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      flex-wrap: wrap;
    }

    .brand a { font-weight: 700; font-size: 18px; }

    .nav-links {
      display: flex;
      gap: 16px;
      font-weight: 600;
      color: var(--text-muted);
    }

    .nav-links a.active { color: var(--primary); }

    .search-bar {
      display: flex;
      gap: 10px;
      padding: 8px 12px;
      border-radius: 12px;
      border: 1px solid var(--border);
      background: white;
    }

    .search-bar input {
      border: none;
      outline: none;
      font-size: 14px;
      width: 180px;
      font-family: inherit;
    }

    .btn {
      border: none;
      border-radius: 10px;
      padding: 8px 12px;
      font-weight: 600;
      cursor: pointer;
      font-family: inherit;
    }

    .btn-primary {
      background: var(--primary);
      color: white;
    }

    main { flex: 1; padding: 36px 24px 64px; }

    .container { max-width: 720px; margin: 0 auto; }

    .panel {
      background: var(--surface);
      border-radius: 20px;
      border: 1px solid var(--border);
      box-shadow: var(--shadow);
      padding: 24px;
    }

    .page-title h1 {
      margin: 0 0 8px;
      font-family: "Space Grotesk", system-ui, sans-serif;
      font-size: clamp(26px, 4vw, 36px);
    }

    .page-title p {
      margin: 0 0 18px;
      color: var(--text-muted);
    }

    .wallet-bar {
      margin-top: 18px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      padding: 12px 14px;
      border-radius: 12px;
      border: 1px solid var(--border);
      background: white;
    }

    .wallet-label { color: var(--text-muted); font-size: 14px; margin-right: 8px; }

    .alert {
      padding: 12px 14px;
      border-radius: 12px;
      border: 1px solid transparent;
      background: rgba(47, 107, 255, 0.08);
      color: #234fbd;
      margin-top: 16px;
    }

    .alert.d-none { display: none; }

    .result-block { margin-top: 16px; }

    #trackingResult p { margin: 6px 0; font-size: 14px; }

    .status-pill {
      display: inline-block;
      margin-top: 12px;
      padding: 6px 12px;
      border-radius: 999px;
      background: rgba(47, 107, 255, 0.12);
      color: var(--primary);
      font-weight: 600;
      font-size: 12px;
    }

    .map-panel {
      margin-top: 18px;
      position: relative;
      overflow: hidden;
    }

    #trackingMap {
      width: 100%;
      height: 260px;
      border-radius: 16px;
    }

    .map-overlay {
      position: absolute;
      left: 16px;
      right: 16px;
      bottom: 16px;
      background: rgba(255, 255, 255, 0.92);
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 10px 12px;
      color: var(--text-muted);
      box-shadow: var(--shadow);
    }

    .map-overlay strong { color: var(--text); }

    .site-footer {
      border-top: 1px solid var(--border);
      background: white;
    }

    .footer-inner {
      max-width: 1100px;
      margin: 0 auto;
      padding: 16px 24px;
      display: flex;
      justify-content: space-between;
      gap: 12px;
      flex-wrap: wrap;
      color: var(--text-muted);
      font-size: 13px;
    }

    .footer-links {
      display: flex;
      gap: 12px;
    }
  </style>
</head>

<body>
  <div class="page">
    <%- include("partials/header", { activePage: "track" }) %>

    <main>
      <div class="container">
        <section class="panel">
          <div class="page-title">
            <h1>Tracking: <%= trackingId %></h1>
            <p>Connect your wallet to load tracking data from the blockchain.</p>
          </div>

          <div class="wallet-bar">
            <div>
              <span class="wallet-label">Account:</span>
              <strong id="accountAddress">Not connected</strong>
            </div>
            <button class="btn btn-primary" id="connectButton">Connect Wallet</button>
          </div>

          <div class="alert d-none" id="statusMessage"></div>

          <div class="result-block">
            <h3 id="detailTitle">Tracking Details</h3>
            <p id="detailOrderId">Tracking ID: <%= trackingId %></p>
            <div id="trackingResult">
              <p><strong>Sender:</strong> -</p>
              <p><strong>Sender Phone:</strong> -</p>
              <p><strong>Receiver:</strong> -</p>
              <p><strong>Pickup:</strong> -</p>
              <p><strong>Drop Off:</strong> -</p>
              <p><strong>Status:</strong> -</p>
            </div>
            <span class="status-pill" id="detailStatus">Loading</span>
            <p id="routeSummary"></p>
          </div>
        </section>

        <section class="panel map-panel">
          <div id="trackingMap" aria-label="Delivery route map"></div>
          <div class="map-overlay">
            <strong>Live delivery route</strong>
            <p id="routeSummaryMap" style="margin:6px 0 0;"></p>
          </div>
        </section>
      </div>
    </main>

    <%- include("partials/footer") %>
  </div>

  <script
    src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"
    integrity="sha256-20nQCchB9co0qIjJZRGuk2/Z9VM+kNiyxNV1lvTlZBo="
    crossorigin=""
  ></script>
  <script src="https://cdn.jsdelivr.net/npm/web3@4.16.0/dist/web3.min.js"></script>
  <script>
    window.TRACKING_ID = "<%= trackingId %>";
  </script>
  <script src="/js/marketplace-client.js"></script>
</body>
</html>
